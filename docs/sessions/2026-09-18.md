## 09:07 — Study Corner redesign rollout
- did: Rebuilt the Study Corner resource flow into a guided Branch → Level → Subject → Resources stepper (`ResourceBrowser.vue`) backed by new data modules `src/data/study/curriculum.js` and `src/data/study/resources.js`; added `ResourceResults.vue` (All/Lectures/Notes/PYQs filters, curated folders pinned, notes-by-author, PYQs-by-year, show-all) and the decorative `StudyNook.vue` band; shortened the Study hero via `PageHero compact` + scoped overrides; decoupled the shared search out of `StudyView.vue`. Rewrote the standalone scratch mirror `emtyT/studycorner.html` as a faithful single-file port (real fonts/navbar/hero/stepper/nook/tools/contribute/footer) with editable sample data.
- changed: `src/components/study/ResourceBrowser.vue`, `ResourceResults.vue`, `StudyNook.vue`, `src/views/StudyView.vue`, `src/data/study/{curriculum,resources}.js`, `CHANGELOG.md` (new), `emtyT/studycorner.html` (untracked scratch).
- decided: Level picker becomes cards matching branch cards; search folds into the subject step; step scroll uses `block: nearest` with `scroll-margin`; DS-only resources today with an ask-for-missing-resources empty state.
- next: Owner visual pass at #/study; optional Playwright Chromium install + smoke test (ask owner).

## 09:38 — Doubts Board removed
- did: Deleted the anonymous Doubts Board (`src/components/study/DoubtsBoard.vue`) and its seed data (`public/data/doubts.json`) on owner request; removed the usage/import from `StudyView.vue`, updated README tree + CHANGELOG (added **Removed** section), and stripped the matching CSS/HTML/data/JS out of `emtyT/studycorner.html`. Verified `npm run lint` and `npm run build` pass and the scratch page's JS still parses.
- changed: `src/views/StudyView.vue`, `README.md`, `CHANGELOG.md`, `emtyT/studycorner.html`; deleted `src/components/study/DoubtsBoard.vue`, `public/data/doubts.json`.
- decided: Remove rather than repurpose; page tone rhythm is now Hero · Browser A · Nook A · Tools A · Contribute B.
- next: Owner re-verifies #/study; footer text is now "Affiliated to Indian Institute Of Technology, Madras" (both live component and scratch page).

## 09:45 — Footr + docs housekeeping
- did: Rewrote the footer Affiliate line to "Affiliated to Indian Institute Of Technology, Madras" in `src/components/AppFooter.vue` and the scratch page; created `CHANGELOG.md` (Keep-a-Changelog style, Unreleased sections for shipped work + scratch scaffold); refreshed `docs/STATE.md` for the Study Corner state, decisions and gotchas.
- changed: `src/components/AppFooter.vue`, `CHANGELOG.md` (created), `docs/STATE.md`, `emtyT/studycorner.html`.
- next: Run `/checkpoint` for the session; PR for the Study Corner work.